<RelativeLayout xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:tools="http://schemas.android.com/tools"
    android:layout_width="match_parent"
    android:layout_height="match_parent"
    android:background="#5a9965"
    android:paddingBottom="@dimen/activity_vertical_margin"
    android:paddingLeft="@dimen/activity_horizontal_margin"
    android:paddingRight="@dimen/activity_horizontal_margin"
    android:paddingTop="@dimen/activity_vertical_margin"
    tools:context=".MainActivity" >

    <TextView
        android:id="@+id/textView1"
        android:layout_width="wrap_content"
        android:layout_height="wrap_content"
        android:layout_alignParentTop="true"
        android:layout_centerHorizontal="true"
         android:layout_marginTop="40dp"
        android:text="Main Menu"
        android:textColor="#c9e9d3"
        android:textSize="40sp" />

    <ImageButton
        android:id="@+id/user_manual_btn"
        android:layout_width="240dp"
        android:layout_height="50dp"
        android:layout_below="@+id/textView1"
        android:layout_centerHorizontal="true"
        android:layout_marginTop="40dp"
        android:contentDescription="@null"
        android:src="@drawable/custom_user_manual" />

    <ImageButton
        android:id="@+id/start_conversation_btn"
        android:layout_width="240dp"
        android:layout_height="50dp"
        android:layout_alignLeft="@+id/user_manual_btn"
        android:layout_below="@+id/user_manual_btn"
        android:layout_marginTop="20dp"
        android:contentDescription="@null"
        android:src="@drawable/custom_start_convo" />

    <ImageButton
        android:id="@+id/videos_btn"
        android:layout_width="240dp"
        android:layout_height="50dp"
        android:layout_alignLeft="@+id/start_conversation_btn"
        android:layout_below="@+id/start_conversation_btn"
        android:layout_marginTop="20dp"
        android:contentDescription="@null"
        android:src="@drawable/custom_videos" />

    <ImageButton
        android:id="@+id/about_app_btn"
        android:layout_width="240dp"
        android:layout_height="50dp"
        android:layout_alignLeft="@+id/videos_btn"
        android:layout_below="@+id/videos_btn"
        android:layout_marginTop="20dp"
        android:contentDescription="@null"
        android:src="@drawable/custom_about_app" />

</RelativeLayout>